# Pinned to exact versions for reproducible walled-garden builds.
# Update via: .venv/bin/pip freeze | grep -iE '^(...)==' from a working venv,
# then re-test the full suite before committing.

# FastAPI BFF
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart==0.0.20
pydantic==2.13.3

# Pipeline (mirrors AIC_PHASE1 — keep versions in sync with upstream
# when the vendored ml_package / phase3_package gets refreshed).
pandas==2.3.3
numpy==2.0.2
openpyxl==3.1.5
xlsxwriter==3.2.9
nltk==3.9.2
rank-bm25==0.2.2
rapidfuzz==3.13.0
# xgboost retired in favour of LinearSVC — re-add to revive the XGB backend (see ml_classifier._make_classifier)
scikit-learn==1.6.1
regex==2026.1.15
python-dateutil==2.9.0.post0

# Tests
pytest==8.4.2
httpx==0.28.1
